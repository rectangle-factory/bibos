## Development

Install [Foundry](https://github.com/gakonst/foundry/).

Test

```
forge test --ffi
```

Build

```
forge build
```

`npm install` for prettier

For git submodules:

```[bash]
    git submodule init
    git submodule update
```


Install `jq`:
```
    brew install jq
```